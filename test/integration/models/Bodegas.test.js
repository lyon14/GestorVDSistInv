/*const util = require('util');

describe('Bodegas (model)', function() {
  describe('#find()', function() {
    it('deberia retornar todas las bodegas', function(done) {
      Bodegas.find()
        .then(function(results) {
          // some tests
          if (results.length === 0) {
            return done(new Error(
              'No hay bodegas en la base de datos.'
            ));
          }
          return done();
        })
        .catch(done);
    });
  });
  describe('#create()', function() {
    it('deberia crear una bodega', function(done) {
      Bodegas.create({nombre: 'Bodega 1', direccion: 'Direccion 1'})
        .then(function(results) {
          // some tests
          if (!results) {
            return done(new Error(
              'No se pudo crear la bodega.'
            ));
          }
          return done();
        })
        .catch(done);
    });
  });
});
*/
